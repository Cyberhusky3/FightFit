import { skillTrees } from '@/lib/data';import { Card, Pill } from '@/components/ui';
export default function SkillTrees(){return <main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-black">Skill Trees</h1><p className="mt-2 text-zinc-400">Unlock fundamentals in order, then branch into combinations and strategy.</p><div className="mt-6 grid gap-5 md:grid-cols-2">{Object.entries(skillTrees).map(([name,skills])=><Card key={name}><h2 className="text-2xl font-black capitalize">{name.replace('muayThai','Muay Thai')}</h2><div className="mt-5 flex flex-col items-center gap-2">{skills.map((s,i)=><div key={s} className="contents"><div className={`rounded-2xl px-5 py-3 font-bold ${i<3?'bg-primary text-black':'bg-white/10 text-white'}`}>{s}</div>{i<skills.length-1&&<span className="text-primary">↓</span>}</div>)}</div><Pill>{skills.slice(0,3).length} unlocked</Pill></Card>)}</div></main>}
